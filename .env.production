# Frontend en Cloudflare Workers (smlgmesait.workers.dev) + API en Railway → CORS cross-origin.
# Apunta a la URL absoluta de la API en Railway.
VITE_API_URL=https://mesa-it-production.up.railway.app/api
